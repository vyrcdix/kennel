node_modules
dist
dist-ssr
*.local
.vite

# TS build info (regenerated on every build)
*.tsbuildinfo

# Per-user Claude Code settings (permissions etc.)
.claude/settings.local.json

# Local operator notes — server IPs, hostnames, account specifics. Never tracked.
private-notes.md

# Local secrets (Anthropic key, MCP token, etc.) loaded by Node's --env-file.
# Prod reads the equivalent file via systemd EnvironmentFile=/etc/kennel/secrets.env.
.env
.env.*
!.env.example

# Server runtime artifacts
server/kennel.db
server/kennel.db.bak
server/kennel.db-journal
server/kennel.db-wal
server/kennel.db-shm
server/content/
server/dist/

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
